<?xml version="1.0" encoding="utf-8"?>
<odoo noupdate="1">
    <record id="ir_cron_vendor_catalog_all" model="ir.cron">
        <field name="name">Import Catálogo - Todos los proveedores</field>
        <field name="model_id" ref="model_vendor_catalog_config"/>
        <field name="state">code</field>
        <field name="code">model._cron_run_all()</field>
        <field name="interval_number">6</field>
        <field name="interval_type">hours</field>
        <field name="active">True</field>
        <field name="nextcall" eval="(DateTime.now() + relativedelta(minutes=5)).strftime('%Y-%m-%d %H:%M:%S')"/>
        <field name="user_id" ref="base.user_root"/>
    </record>
</odoo>
